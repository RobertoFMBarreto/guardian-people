// import 'fence.dart';

// class Fences {
//   static List<Fence> searchFences(String searchString, List<Fence> allFences) {
//     List<Fence> result = [];
//     if (searchString.isNotEmpty) {
//       result = allFences
//           .where(
//             (element) => element.name.toString().contains(searchString),
//           )
//           .toList();
//     } else {
//       // reset devices
//       // add all devices again
//       result.addAll(allFences);
//     }
//     return result;
//   }
// }
