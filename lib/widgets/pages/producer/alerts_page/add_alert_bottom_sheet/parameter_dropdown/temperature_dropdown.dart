import 'package:flutter/material.dart';

class TemperatureDropdown extends StatelessWidget {
  const TemperatureDropdown({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
